<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.module.navigation.services.domain.dao.QueryNavigationDao">
    <sql id="Base_Column_List">
        t_navigation
        .
        id
        , t_navigation.name, t_navigation.url, t_navigation.title, t_navigation.desc,t_navigation.org_id
        ,t_navigation.path
    </sql>


    <select id="queryMasterModel" resultType="NavigationModel">
        SELECT t_navigation.*
        FROM t_navigation
        WHERE t_navigation.id = #{id}
        GROUP BY t_navigation.id
    </select>


    <select id="findNavigationPage" resultType="NavigationRecord" parameterType="NavigationRecord">
        SELECT
        <include refid="Base_Column_List"/>


        FROM t_navigation


        WHERE 1=1


        <if test="record.id != null and record.id>0 ">
            AND t_navigation.id LIKE CONCAT('%',#{record.id},'%')
        </if>
        <if test="record.name != null and record.name!= ''">
            AND t_navigation.name LIKE CONCAT('%',#{record.name},'%')
        </if>
        <if test="record.url != null and record.url!= ''">
            AND t_navigation.url LIKE CONCAT('%',#{record.url},'%')
        </if>
        <if test="record.title != null and record.title!= ''">
            AND t_navigation.title LIKE CONCAT('%',#{record.title},'%')
        </if>
        <if test="record.desc != null and record.desc!= ''">
            AND t_navigation.desc LIKE CONCAT('%',#{record.desc},'%')
        </if>

        <if test="startTime != null">
            <![CDATA[AND t_navigation.start_time >= DATE(#{startTime})]]>
        </if>
        <if test="endTime != null">
            <![CDATA[AND t_navigation.end_time <= DATE(#{endTime}]]>
        </if>
        <!--
    <if test="search != null and search != ''">
        OR t_navigation.name LIKE CONCAT('%',#{search},'%')
    </if>
    -->
    </select>
</mapper>