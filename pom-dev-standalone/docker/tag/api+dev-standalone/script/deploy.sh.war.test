
export ROLLBACK_KEEP_NUM=2
webapp=ROOT.war

target=/e/aliyun.com/dockers/alliance/biliya/mall-webapp/target
cp deploy.sh $target
cp predeploy.sh $target

cd $target
if [ ! -f $webapp ];then
  cp *.war $webapp
fi
export PATH=$PATH:./

pwd
ls
echo ====================================
sh deploy.sh
echo !====================================!

ls
